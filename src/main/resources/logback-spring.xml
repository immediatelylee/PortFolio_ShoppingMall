<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- ============================================ -->
    <!-- 1. 변수 설정 (로그 파일 위치와 이름 정의) -->
    <!-- ============================================ -->
    <property name="LOG_PATH" value="./logs"/>
    <property name="WEBLOG_FILE_NAME" value="weblog"/>
    <property name="BUSINESS_FILE_NAME" value="business"/>
    <property name="ERROR_FILE_NAME" value="error"/>

    <!-- ============================================ -->
    <!-- 2. 콘솔 출력 설정 (개발 시 확인용) -->
    <!-- ============================================ -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <!-- ============================================ -->
    <!-- 3. 웹 액세스 로그 파일 설정  -->
    <!-- ============================================ -->

    <!-- WEB 액세스 로그(JSON) -->
    <appender name="WEBLOG_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_PATH}/${WEBLOG_FILE_NAME}.log</file>
        <encoder class="net.logstash.logback.encoder.LogstashEncoder">
            <!-- MDC를 최상위 필드로 -->
            <includeMdcKeyName>trace_id</includeMdcKeyName>
            <includeMdcKeyName>user_id</includeMdcKeyName>
            <includeMdcKeyName>session_id</includeMdcKeyName>
            <includeMdcKeyName>remote_ip</includeMdcKeyName>
            <includeMdcKeyName>method</includeMdcKeyName>
            <includeMdcKeyName>uri</includeMdcKeyName>
            <includeMdcKeyName>status</includeMdcKeyName>
            <includeMdcKeyName>duration_ms</includeMdcKeyName>
            <includeMdcKeyName>page_type</includeMdcKeyName>
            <includeMdcKeyName>product_id</includeMdcKeyName>
            <includeMdcKeyName>category_id</includeMdcKeyName>
            <includeMdcKeyName>search_keyword</includeMdcKeyName>
            <includeMdcKeyName>action</includeMdcKeyName>
            <includeMdcKeyName>is_authenticated</includeMdcKeyName>
            <includeMdcKeyName>user_agent</includeMdcKeyName>
            <includeMdcKeyName>referer</includeMdcKeyName>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${LOG_PATH}/${WEBLOG_FILE_NAME}-%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <timeBasedFileNamingAndTriggeringPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedFNATP">
                <maxFileSize>100MB</maxFileSize>
            </timeBasedFileNamingAndTriggeringPolicy>
            <maxHistory>30</maxHistory>
            <totalSizeCap>10GB</totalSizeCap>
        </rollingPolicy>
        <immediateFlush>true</immediateFlush>
    </appender>

    <!-- ============================================ -->
    <!-- 4. 비즈니스 이벤트 로그 파일 설정 -->
    <!-- ============================================ -->

    <!-- BUSINESS 이벤트 로그(JSON) -->
    <appender name="BUSINESS_LOG_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_PATH}/${BUSINESS_FILE_NAME}.log</file>
        <encoder class="net.logstash.logback.encoder.LogstashEncoder">
            <!-- 공통 -->
            <includeMdcKeyName>timestamp</includeMdcKeyName>
            <includeMdcKeyName>event_type</includeMdcKeyName>
            <includeMdcKeyName>app</includeMdcKeyName>
            <includeMdcKeyName>trace_id</includeMdcKeyName>
            <includeMdcKeyName>session_id</includeMdcKeyName>
            <!-- 로그인 관련 -->
            <includeMdcKeyName>user_id</includeMdcKeyName>
            <includeMdcKeyName>login_email</includeMdcKeyName>
            <includeMdcKeyName>login_method</includeMdcKeyName>
            <includeMdcKeyName>success</includeMdcKeyName>
            <includeMdcKeyName>reason</includeMdcKeyName>
            <includeMdcKeyName>remote_ip</includeMdcKeyName>
            <includeMdcKeyName>referer</includeMdcKeyName>
            <!-- 도메인 키 -->
            <includeMdcKeyName>product_id</includeMdcKeyName>
            <includeMdcKeyName>order_id</includeMdcKeyName>
            <includeMdcKeyName>amount</includeMdcKeyName>
            <includeMdcKeyName>payment_method</includeMdcKeyName>

            <!-- 장바구니/주문 상세 -->
            <includeMdcKeyName>quantity</includeMdcKeyName>
            <includeMdcKeyName>unit_price</includeMdcKeyName>
            <includeMdcKeyName>total_price</includeMdcKeyName>

        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${LOG_PATH}/${BUSINESS_FILE_NAME}-%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <timeBasedFileNamingAndTriggeringPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedFNATP">
                <maxFileSize>100MB</maxFileSize>
            </timeBasedFileNamingAndTriggeringPolicy>
            <maxHistory>90</maxHistory>
            <totalSizeCap>20GB</totalSizeCap>
        </rollingPolicy>
    </appender>

    <!-- ============================================ -->
    <!-- 5. 에러 로그 파일 설정 -->
    <!-- ============================================ -->

    <!-- ERROR 전용 -->
    <appender name="ERROR_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_PATH}/${ERROR_FILE_NAME}.log</file>
        <filter class="ch.qos.logback.classic.filter.LevelFilter">
            <level>ERROR</level>
            <onMatch>ACCEPT</onMatch>
            <onMismatch>DENY</onMismatch>
        </filter>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n%ex{full}</pattern>
            <charset>UTF-8</charset>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${LOG_PATH}/${ERROR_FILE_NAME}-%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>90</maxHistory>
        </rollingPolicy>
    </appender>


    <!-- ============================================ -->
    <!-- 6. 비동기 Appender -->
    <!-- ============================================ -->
    <!-- 비동기 -->
    <appender name="ASYNC_WEBLOG" class="ch.qos.logback.classic.AsyncAppender">
        <appender-ref ref="WEBLOG_FILE"/>
        <queueSize>512</queueSize>
        <discardingThreshold>0</discardingThreshold>
        <includeCallerData>false</includeCallerData>
    </appender>

    <appender name="ASYNC_BUSINESS" class="ch.qos.logback.classic.AsyncAppender">
        <appender-ref ref="BUSINESS_LOG_FILE"/>
        <queueSize>512</queueSize>
        <discardingThreshold>0</discardingThreshold>
    </appender>


    <!-- ============================================ -->
    <!-- 7. 로거 설정 (어떤 로거가 어떤 파일에 기록할지) -->
    <!-- ============================================ -->

    <!-- WebLogFilter의 로그는 WEBLOG_FILE로 -->
    <logger name="com.shoppingmall.project_shoppingmall.filter.WebLogFilter" level="INFO" additivity="false">
        <appender-ref ref="ASYNC_WEBLOG"/>
    </logger>

    <!-- BusinessEventLogger의 로그는 BUSINESS_LOG_FILE로 -->
    <logger name="BUSINESS_LOG" level="INFO" additivity="false">
        <appender-ref ref="ASYNC_BUSINESS"/>
    </logger>

    <!-- SQL 쿼리 로깅 (선택사항 - 디버깅용) -->
    <logger name="org.hibernate.SQL" level="DEBUG" additivity="false">
        <appender-ref ref="CONSOLE"/>
    </logger>
    <logger name="org.hibernate.type.descriptor.sql.BasicBinder" level="TRACE" additivity="false">
        <appender-ref ref="CONSOLE"/>
    </logger>

    <!-- ============================================ -->
    <!-- 8. 루트 로거 (기본 설정) -->
    <!-- ============================================ -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="ERROR_FILE"/>
    </root>

    <!-- ============================================ -->
    <!-- 설명 -->
    <!-- ============================================ -->
    <!--
    이 설정 파일이 하는 일:

    1. logs/ 폴더에 3가지 종류의 로그 파일 생성
       - weblog.log: 모든 HTTP 요청/응답
       - business.log: 중요한 비즈니스 이벤트
       - error.log: 에러 정보

    2. 로그 파일 자동 관리
       - 매일 새로운 파일 생성 (weblog-2025-10-29.log)
       - 파일 크기가 100MB 넘으면 분할 (weblog-2025-10-29.0.log, .1.log, ...)
       - 오래된 로그 파일 자동 삭제 (30일 또는 90일)

    3. JSON 형식으로 저장
       - Elasticsearch에서 쉽게 파싱 가능
       - 구조화된 데이터 분석 용이

    4. 성능 최적화
       - 비동기 처리로 애플리케이션 성능 영향 최소화
    -->

</configuration>